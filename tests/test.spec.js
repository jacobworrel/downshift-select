import chai from 'chai';
import { mount } from 'enzyme';
import React from 'react';

chai.should();

describe('test', () => {
  it(`should `, () => {
    const test = false;
    test.should.equal(false);
  });
});
